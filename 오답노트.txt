================================================================================
배포 환경 오류 및 해결 - 최종 정리
================================================================================

❌ 문제 1: SQLite 데이터베이스 파일 생성 실패
--------------------------------------------------------------------------------
원인: SQLx는 기본적으로 데이터베이스 파일이 없으면 자동 생성하지 않음
에러: error returned from database: (code: 14) unable to open database file
해결: SqliteConnectOptions::create_if_missing(true) 추가
코드: agent/src/db/queries.rs:12-14

❌ 문제 2: 데이터베이스 경로 충돌
--------------------------------------------------------------------------------
원인: /data/db/ 디렉토리와 /data/db.sqlite 파일명 충돌
시도1: /data/db.sqlite → 실패 (기존 /data/db/ 디렉토리와 충돌)
시도2: /data/db/easycicd.db → 실패 (디렉토리 구조 혼란)
해결: 전용 디렉토리 생성 /data/easycicd/db.sqlite
추가: main.rs에 std::fs::create_dir_all("/data/easycicd") 추가

❌ 문제 3: Axum 0.7 라우팅 문법 변경
--------------------------------------------------------------------------------
원인: Axum이 0.7에서 경로 파라미터 문법 변경
에러: Path segments must not start with `:`. For capture groups, use `{capture}`
해결: :id → {id} 변경
파일: agent/src/api/projects.rs:20-21, agent/src/api/builds.rs:16

❌ 문제 4: Axum 0.7 정적 파일 서빙 변경
--------------------------------------------------------------------------------
원인: Axum 0.7에서 루트 경로 nest_service 금지
에러: Nesting at the root is no longer supported. Use fallback_service instead
해결: .nest_service("/", ...) → .fallback_service(...)
파일: agent/src/main.rs:71

❌ 문제 5: GLIBC 버전 불일치
--------------------------------------------------------------------------------
원인: 빌드 단계(Trixie)와 런타임 단계(Bookworm)의 GLIBC 버전 차이
에러: /lib/x86_64-linux-gnu/libc.so.6: version 'GLIBC_2.39' not found
해결: 런타임 베이스 이미지를 debian:trixie-slim으로 변경
파일: agent/Dockerfile:29

❌ 문제 6: 정적 파일 경로 오류
--------------------------------------------------------------------------------
원인: 작업 디렉토리가 /app인데 ../frontend 참조 시 /frontend가 됨
실제: /app/frontend 디렉토리가 볼륨 마운트됨
해결: "../frontend" → "frontend" 변경
파일: agent/src/main.rs:71

❌ 문제 7: nginx 설정 - daemon directive 중복
--------------------------------------------------------------------------------
원인: nginx.conf에 "daemon off;" 포함, runtime_command에도 "-g 'daemon off;'" 포함
에러: nginx: [emerg] "daemon" directive is duplicate in /app/nginx.conf:1
영향: Runtime 컨테이너가 계속 재시작하며 헬스체크 실패
해결:
  1. nginx.conf에만 "daemon off;" 유지
  2. runtime_command에서 "-g 'daemon off;'" 제거
  3. detector.rs의 모든 nginx 관련 설정 수정
파일:
  - agent/src/github/detector.rs:125 (Node.js Frontend)
  - agent/src/github/detector.rs:224 (Static Site)
  - agent/src/github/detector.rs:425 (기존에 수정된 곳)

❌ 문제 8: Docker 네트워크 통신 실패 - localhost 사용
--------------------------------------------------------------------------------
원인: Agent와 Runtime 컨테이너가 서로 다른 네트워크 관점에서 localhost 사용
배경:
  - Agent 컨테이너: easycicd_easycicd 네트워크 (gateway: 172.19.0.1)
  - Runtime 컨테이너: 처음에는 별도 네트워크
  - 헬스체크/프록시에서 localhost 사용 시 자기 자신을 가리킴

문제 8-1: 헬스체크 실패
에러: Health check failed - error sending request for url (http://localhost:10001/health)
원인: Agent가 localhost:10001 호출 시 자기 자신을 가리킴
시도1: 172.17.0.1 사용 → 실패 (bridge 네트워크, agent는 easycicd 네트워크)
시도2: host 네트워크 모드 → 실패 (포트 바인딩 안됨)
해결:
  1. Runtime 컨테이너를 easycicd_easycicd 네트워크에 연결
  2. Gateway IP (172.19.0.1) 자동 감지
  3. 헬스체크에서 gateway_ip 사용
파일:
  - agent/src/docker/client.rs:60-72 (gateway IP 감지)
  - agent/src/docker/client.rs:293-304 (네트워크 연결)
  - agent/src/build/deployer.rs:226-227 (헬스체크 URL)

문제 8-2: 프록시 연결 실패
에러: Failed to proxy request: error sending request for url (http://localhost:10001/)
원인: Reverse proxy에서 localhost:10001 호출 시 자기 자신을 가리킴
해결:
  1. AppState에 gateway_ip 필드 추가
  2. main.rs에서 DockerClient 초기화 후 gateway_ip 추출
  3. 프록시 라우터에서 gateway_ip 사용
파일:
  - agent/src/state.rs:16 (gateway_ip 필드 추가)
  - agent/src/state.rs:20 (생성자 파라미터 추가)
  - agent/src/main.rs:63-64 (DockerClient 초기화)
  - agent/src/proxy/router.rs:89-92 (gateway_ip 사용)

네트워크 구조 이해:
  - Cloudflare Tunnel → 172.17.0.1:9999 (또는 172.19.0.1:9999)
  - 포트 바인딩: 0.0.0.0:9999→8080 (모든 네트워크에서 접근 가능)
  - Agent 내부: 172.19.0.1:10001로 프록시 (같은 네트워크 내 통신)
  - Runtime 컨테이너: easycicd_easycicd + bridge 네트워크 (멀티 네트워크)

✅ 최종 해결 사항
--------------------------------------------------------------------------------
1. SQLite 자동 생성: create_if_missing(true)
2. 데이터 경로: /data/easycicd/db.sqlite, /data/easycicd/logs/
3. Axum 라우팅: {id} 문법, fallback_service 사용
4. GLIBC 통일: debian:trixie-slim
5. 정적 파일: frontend 상대 경로
6. 포트 설정: 웹 UI 10000, 리버스 프록시 9999, 프로젝트 10000부터 시작
7. nginx 설정: daemon off는 nginx.conf에만
8. 네트워크 통신: Gateway IP (172.19.0.1) 자동 감지 및 사용
9. Runtime 네트워크: easycicd_easycicd 네트워크 연결
10. 모노레포 working_directory: path_filter로부터 추출, 빌드 컨테이너 WORKDIR 동적 설정

❌ 문제 9: 모노레포 빌드 실패 - working_directory DB 저장 누락
--------------------------------------------------------------------------------
원인: queries.rs의 INSERT 쿼리에 working_directory 컬럼 및 바인딩 파라미터 누락
증상:
  - Gradle: "Directory '/app' does not contain a Gradle build"
  - npm: "ENOENT: no such file or directory, open '/app/package.json'"
  - 모노레포 프로젝트에서 path_filter="project1" 설정했으나 빌드는 /app에서 실행됨

근본 원인:
  1. 프론트엔드(Setup.svelte)에서 working_directory 전송 O
  2. API(projects.rs)에서 working_directory 수신 O
  3. CreateProject 구조체에 working_directory 필드 O
  4. INSERT 쿼리에 working_directory 컬럼 X ← 문제
  5. 결과: 모든 프로젝트 working_directory = NULL
  6. 빌드 컨테이너는 항상 /app에서 실행
  7. 모노레포 하위 프로젝트(project1/)의 package.json/build.gradle 찾지 못함

해결:
  1. INSERT 쿼리에 working_directory 컬럼 추가
  2. VALUES에 ? 플레이스홀더 추가 (13개에서 14개로)
  3. .bind(&project.working_directory) 추가
파일: agent/src/db/queries.rs:89-110

관련 수정 이력:
  - 004_working_directory.sql 마이그레이션 생성
  - models.rs에 working_directory 필드 추가 (Project, CreateProject)
  - docker/client.rs에서 WORKDIR 동적 설정
  - github/detector.rs에서 path_filter로부터 working_directory 추출
  - build/executor.rs에서 working_directory 전달
  - frontend Setup.svelte에서 working_directory 전송
  - api/projects.rs에서 working_directory 수신 및 전달
  - queries.rs의 migrate()에 migration4 추가

교훈: 데이터 플로우 전체를 확인해야 함 (Frontend → API → DB → Build Executor → Docker)

❌ 문제 10: 헬스체크 실패 - stale data 문제
--------------------------------------------------------------------------------
원인: 헬스체크에서 이미 업데이트된 컨테이너 ID를 확인하지 못함
배경:
  - 배포 플로우: 새 컨테이너 시작 → DB에 컨테이너 ID 저장 → 헬스체크 수행
  - deployer.rs의 deploy() 함수에서 project 변수는 함수 시작 시점의 값
  - DB에 새 컨테이너 ID를 업데이트해도 project 변수는 여전히 old 값 유지
  - 헬스체크가 old 컨테이너 ID를 체크하여 실패

증상:
  - 새 컨테이너가 정상 실행 중인데도 헬스체크 실패
  - active_slot이 전환되지 않음
  - 프록시 연결 실패 (이전 슬롯의 컨테이너를 가리킴)

문제 코드 (deployer.rs):
```rust
// 새 컨테이너 시작
let container_id = self.docker.run_runtime_container(...).await?;

// DB 업데이트
match target_slot {
    Slot::Blue => {
        self.state.db.update_project_blue_container(project.id, Some(container_id.clone())).await?;
    }
    ...
}

// ❌ 문제: project는 여전히 old container_id를 가지고 있음
let health_check_result = self.perform_health_check(project, build, target_port).await;

// perform_health_check 내부에서:
let container_id = match target_slot {
    Slot::Blue => project.blue_container_id.as_ref(), // ❌ old value
    ...
}
```

해결:
  1. perform_health_check에 container_id를 직접 전달
  2. project 객체에서 읽지 않고 실제 생성된 container_id 사용
  3. 함수 시그니처 변경:
     - Before: async fn perform_health_check(&self, project: &Project, build: &Build, port: u16)
     - After: async fn perform_health_check(&self, project: &Project, build: &Build, container_id: &str)

파일: agent/src/build/deployer.rs
  - Line 118-120: 헬스체크 호출 시 &container_id 전달
  - Line 252-310: perform_health_check 함수 시그니처 및 내부 로직 수정

교훈:
  - In-memory 객체와 DB 상태의 동기화 주의
  - 중요한 값은 직접 전달하여 stale data 문제 방지
  - Rust의 소유권 시스템은 이런 버그를 컴파일 타임에 잡지 못함 (논리 오류)

❌ 문제 11: Active Slot 전환 실패 - 헬스체크 컨텍스트 패스
--------------------------------------------------------------------------------
원인: HTTP 기반 헬스체크가 특정 URL 경로를 가정
배경:
  - Spring Boot 앱이 context path를 사용하는 경우 (예: /api/spring)
  - 헬스체크 URL: /actuator/health
  - 실제 URL: /api/spring/actuator/health
  - 결과: 404 Not Found로 헬스체크 실패

사용자 질문: "context path를 입력받게 하자는 뜻이였어 빌드 설정창에서. 아니면 헬스체크 로직을 바꿀까. 그냥 컨테이너 잘 떴는지 체크같은 느낌으로."

선택: 헬스체크 로직 변경 (context path 필드 추가하지 않음)

해결:
  1. HTTP 요청 기반 헬스체크 제거
  2. Docker API를 통한 컨테이너 상태 확인으로 변경
  3. is_container_running() 사용 (bollard API)
  4. 10회 재시도, 2초 간격
  5. Context path 설정 불필요

장점:
  - 모든 프레임워크/언어에서 동작 (언어 중립적)
  - Context path, health check endpoint 설정 불필요
  - 단순하고 명확한 로직
  - 컨테이너 시작 직후 바로 확인 가능

파일: agent/src/build/deployer.rs:252-310
```rust
async fn perform_health_check(&self, project: &Project, build: &Build, container_id: &str) -> Result<()> {
    let max_attempts = 10;
    let retry_interval = Duration::from_secs(2);

    for attempt in 1..=max_attempts {
        // Docker API로 컨테이너 상태 확인
        let is_running = self.docker.is_container_running(&container_id).await;

        if is_running {
            info!("Container health check passed on attempt {}/{} - Container is running", attempt, max_attempts);
            return Ok(());
        }

        if attempt < max_attempts {
            sleep(retry_interval).await;
        }
    }

    anyhow::bail!("Health check timed out after {} attempts", max_attempts)
}
```

교훈:
  - 복잡한 설정보다 단순한 해결책 선호
  - 언어/프레임워크 중립적인 방식 선택
  - 사용자 경험 우선 (추가 설정 최소화)

❌ 문제 12: 컨테이너 상태 불일치 - 시스템 재시작 후
--------------------------------------------------------------------------------
원인: 시스템 재시작 후 DB에 저장된 컨테이너 ID가 실제로는 존재하지 않음
배경:
  - Agent 컨테이너 재시작 (docker-compose restart)
  - Runtime 컨테이너들은 삭제됨
  - DB에는 여전히 삭제된 컨테이너 ID 저장
  - 배포 시 존재하지 않는 컨테이너를 참조하여 에러

증상:
  - "Project 'xxx': Blue container abc123 not found, skipping cleanup"
  - Orphaned container references in database
  - 배포 시 불필요한 에러 로그

해결:
  1. 시스템 시작 시 컨테이너 상태 동기화 함수 추가
  2. 모든 프로젝트의 blue/green 컨테이너 확인
  3. 존재하지 않는 컨테이너 ID는 DB에서 제거
  4. 깔끔한 초기 상태로 시작

파일: agent/src/main.rs
```rust
// Line 79-82: 시작 시 동기화 호출
info!("Synchronizing container states...");
synchronize_container_states(&state, &docker).await?;
info!("Container state synchronization complete");

// Line 163-200: 동기화 함수
async fn synchronize_container_states(state: &AppState, docker: &DockerClient) -> Result<()> {
    let projects = state.db.list_projects().await?;

    for project in projects {
        let mut needs_update = false;
        let mut new_blue_id = project.blue_container_id.clone();
        let mut new_green_id = project.green_container_id.clone();

        // Check Blue container
        if let Some(blue_id) = &project.blue_container_id {
            if !docker.is_container_running(blue_id).await {
                info!("Project '{}': Blue container {} not found, clearing from DB", project.name, blue_id);
                new_blue_id = None;
                needs_update = true;
            }
        }

        // Check Green container
        if let Some(green_id) = &project.green_container_id {
            if !docker.is_container_running(green_id).await {
                info!("Project '{}': Green container {} not found, clearing from DB", project.name, green_id);
                new_green_id = None;
                needs_update = true;
            }
        }

        // Update database if needed
        if needs_update {
            state.db.update_project_blue_container(project.id, new_blue_id).await?;
            state.db.update_project_green_container(project.id, new_green_id).await?;
            info!("Project '{}': Container states synchronized", project.name);
        }
    }

    Ok(())
}
```

교훈:
  - 영속성 레이어(DB)와 실제 리소스(컨테이너) 상태 동기화 중요
  - 시스템 시작 시 일관성 검증 필요
  - Garbage collection 같은 정리 작업 자동화

❌ 문제 13: 배포 실패 - 타겟 슬롯에 기존 컨테이너 존재
--------------------------------------------------------------------------------
원인: 타겟 슬롯에 기존 컨테이너가 남아있어 포트 충돌
배경:
  - 이전 배포가 실패한 경우
  - 헬스체크 실패로 롤백하지 않은 경우 (디버깅 모드)
  - 타겟 슬롯에 이미 컨테이너가 실행 중
  - 동일한 포트로 새 컨테이너 시작 시도 → 포트 충돌

사용자 요구사항: "그래 무조건 2개 단위로 할당하고 그걸 DB적으로 관리해보자. 고쳐줘."

해결:
  1. 배포 시작 전에 타겟 슬롯 정리
  2. 기존 컨테이너 확인 → 중지 → 제거
  3. DB에서 컨테이너 ID 제거
  4. 깔끔한 상태에서 새 컨테이너 시작

파일: agent/src/build/deployer.rs:57-83
```rust
// Clean up target slot's old container if it exists
let old_container_id = match target_slot {
    Slot::Blue => project.blue_container_id.as_ref(),
    Slot::Green => project.green_container_id.as_ref(),
};

if let Some(old_id) = old_container_id {
    // Check if container actually exists
    if self.docker.is_container_running(old_id).await {
        info!("Stopping old {} container: {}", target_slot, old_id);
        self.docker.stop_container(old_id).await.ok();
    } else {
        info!("Old {} container {} not found, skipping cleanup", target_slot, old_id);
    }
    // Always try to remove regardless of running state
    self.docker.remove_container(old_id).await.ok();

    // Clear from database
    match target_slot {
        Slot::Blue => {
            self.state.db.update_project_blue_container(project.id, None).await?;
        }
        Slot::Green => {
            self.state.db.update_project_green_container(project.id, None).await?;
        }
    }
}
```

블루-그린 관리 3단계 보호:
  1. 프로젝트 생성: active_slot='Blue', container_ids=NULL
  2. 시스템 시작: 존재하지 않는 컨테이너 DB 정리
  3. 배포 전: 타겟 슬롯 기존 컨테이너 정리

교훈:
  - Idempotent operations (멱등성) 중요
  - 항상 깨끗한 상태에서 시작
  - 실패 시나리오 고려한 robust한 설계
  - DB와 실제 리소스의 일관성 유지

🚀 배포 완료
--------------------------------------------------------------------------------
Docker Hub: choho97/lightweight-ci:latest (sha256:02de4a05b4a3...)
Git Commit: d2b1b04 (2026-01-08)
컨테이너: 정상 실행 중
시스템: 컨테이너 상태 동기화 완료
헬스체크: Docker 컨테이너 상태 확인 방식으로 변경
블루-그린: 3단계 보호 체계 구축 완료

📋 실행 방법
--------------------------------------------------------------------------------
1. docker-compose up -d
2. http://localhost:10000 접속 (웹 UI)
3. http://localhost:9999/{project_name} (배포된 앱)
4. GET /api/settings/webhook-secret로 Webhook Secret 확인

🔍 네트워크 디버깅 명령어
--------------------------------------------------------------------------------
# Gateway IP 확인
docker network inspect easycicd_easycicd | jq '.[0].IPAM.Config[0].Gateway'

# Agent IP 확인
docker inspect easycicd-agent | jq '.[0].NetworkSettings.Networks.easycicd_easycicd.IPAddress'

# Runtime 컨테이너 네트워크 확인
docker inspect tt-green | jq '.[0].NetworkSettings.Networks | keys'

# 프록시 테스트
curl http://localhost:9999/tt/


================================================================================
아키텍처 문제점 (모듈화 리팩토링 필요성)
================================================================================

❌ 문제 14: AppState God Object 패턴
--------------------------------------------------------------------------------
원인: 모든 의존성이 AppState에 집중됨
현재 구조:
  - AppState가 db, event_tx, build_queue, ws_connections, config 등 모두 포함
  - 모든 API 핸들러, 빌드 시스템, 프록시가 AppState에 의존
  - 단위 테스트가 불가능 (전체 AppState 필요)

문제점:
  1. 테스트 어려움: Mock 주입 불가능, 전체 시스템 필요
  2. 높은 결합도: 한 부분 변경 시 전체 영향
  3. 불명확한 의존성: 각 모듈이 실제로 무엇을 필요로 하는지 불명확

예시 (agent/src/api/projects.rs):
```rust
async fn create_project(State(state): State<AppState>, ...) -> impl IntoResponse {
    // state.db, state.docker, state.event_tx 모두 접근 가능
    // 하지만 이 핸들러는 실제로 db만 필요할 수도 있음
}
```

필요한 해결책:
  - Trait 기반 의존성 주입
  - 각 모듈이 필요한 의존성만 받도록 분리
  - AppContext가 서비스들을 조립하되, 서비스는 trait에만 의존

파일: agent/src/state.rs

❌ 문제 15: 비즈니스 로직과 HTTP 핸들러 혼재
--------------------------------------------------------------------------------
원인: API 레이어에 비즈니스 로직이 직접 작성됨
현재 구조:
  - agent/src/api/projects.rs (521 lines)
  - 프로젝트 생성, 삭제, 컨테이너 관리 로직이 모두 API 핸들러에 포함
  - HTTP 요청 처리와 비즈니스 로직이 섞여있음

문제점:
  1. 테스트 어려움: HTTP 레이어 없이는 비즈니스 로직 테스트 불가
  2. 재사용 불가: Webhook, WebSocket 등에서 같은 로직 재사용 어려움
  3. 가독성 저하: 521줄의 파일에서 핵심 로직 찾기 어려움

예시 (agent/src/api/projects.rs:180-220):
```rust
async fn delete_project(State(state): State<AppState>, Path(id): Path<i64>) -> impl IntoResponse {
    // 1. HTTP 요청 파싱
    // 2. 프로젝트 조회
    // 3. 컨테이너 중지/삭제 (비즈니스 로직!)
    // 4. 파일 시스템 정리 (비즈니스 로직!)
    // 5. DB 삭제
    // 6. HTTP 응답 생성
    // 모두 한 함수에 혼재
}
```

필요한 해결책:
  - Application Service 레이어 분리 (ProjectService, BuildService 등)
  - API 핸들러는 요청 파싱 + 서비스 호출 + 응답 생성만 담당
  - 비즈니스 로직은 서비스 레이어에서 독립적으로 테스트

파일: agent/src/api/projects.rs, agent/src/api/builds.rs

❌ 문제 16: 이벤트 발행 코드 분산
--------------------------------------------------------------------------------
원인: 이벤트 발행이 여러 모듈에 흩어져 있음
현재 구조:
  - agent/src/build/executor.rs: 빌드 이벤트 발행
  - agent/src/build/deployer.rs: 배포 이벤트 발행
  - agent/src/api/webhook.rs: Webhook 이벤트 발행
  - 각 모듈이 직접 state.event_tx.send() 호출

문제점:
  1. 로깅 불가: 어디서 어떤 이벤트가 발행되는지 추적 어려움
  2. 중복 코드: 이벤트 발행 전후 로직 중복
  3. 에러 처리 일관성 부족: 각 모듈마다 다른 방식으로 에러 처리

예시 (agent/src/build/executor.rs):
```rust
// 이벤트 발행만 하고 로깅 없음
let _ = self.state.event_tx.send(Event::BuildStatus { ... });
```

필요한 해결책:
  - EventBus trait 정의
  - 모든 이벤트 발행을 EventBus를 통해 수행
  - BoundaryLogger에서 모든 이벤트 발행 로깅
  - 중앙화된 에러 처리

파일: agent/src/events.rs, agent/src/build/executor.rs, agent/src/build/deployer.rs

❌ 문제 17: 테스트 불가능한 구조
--------------------------------------------------------------------------------
원인: Mock 주입이 불가능한 설계
현재 구조:
  - BuildExecutor, Deployer가 직접 DockerClient를 생성
  - 테스트 시 실제 Docker daemon 필요
  - 단위 테스트 작성 불가능

문제점:
  1. 느린 테스트: 실제 Docker 컨테이너 실행 필요
  2. 불안정한 테스트: 네트워크, Docker daemon 상태에 의존
  3. CI/CD 어려움: Docker-in-Docker 환경 필요

예시 (agent/src/build/executor.rs):
```rust
pub struct BuildExecutor {
    state: Arc<AppState>,
    docker: DockerClient, // 직접 생성, Mock 주입 불가
}
```

필요한 해결책:
  - ContainerRuntime trait 정의
  - DockerClient가 trait 구현
  - MockContainerRuntime 작성
  - 서비스 생성 시 trait을 주입

파일: agent/src/build/executor.rs, agent/src/build/deployer.rs, agent/src/docker/client.rs

❌ 문제 18: 모듈 간 통신 로깅 부재
--------------------------------------------------------------------------------
원인: 모듈 간 호출이 로깅되지 않음
현재 구조:
  - API → BuildService 호출: 로깅 없음
  - BuildService → DockerClient 호출: 로깅 없음
  - BuildService → Database 호출: 로깅 없음
  - 전체 플로우 추적 불가능

문제점:
  1. 디버깅 어려움: 문제 발생 시 어느 단계에서 실패했는지 파악 어려움
  2. 성능 분석 불가: 각 단계별 소요 시간 측정 불가
  3. 운영 어려움: 프로덕션 환경에서 문제 추적 어려움

예시 (현재 로그):
```
[INFO] Starting build
[INFO] Build completed
// 중간 과정이 모두 블랙박스
```

필요한 로그 (목표):
```
[10:15:23] [INFO] [abc-123] [API→BuildService] execute_build [ENTRY] build_id=42
[10:15:23] [INFO] [abc-123] [BuildService→BuildRepo] get [CALL]
[10:15:23] [INFO] [abc-123] [BuildService→BuildRepo] get [←DONE] 5ms
[10:15:23] [INFO] [abc-123] [BuildService→Docker] run_build [EXT→]
[10:15:45] [INFO] [abc-123] [BuildService→Docker] run_build [←DONE] 22000ms
[10:15:45] [INFO] [abc-123] [API→BuildService] execute_build [←DONE] 22050ms
```

필요한 해결책:
  - BoundaryLogger 구현 (모든 모듈 경계에서 로깅)
  - Trace ID 생성 및 전파
  - API 미들웨어에서 trace ID 추가
  - 모든 서비스 호출 시 로깅

파일: 전체 (리팩토링 필요)

❌ 문제 19: 데이터베이스 접근 분산
--------------------------------------------------------------------------------
원인: 여러 모듈에서 직접 DB 쿼리 실행
현재 구조:
  - agent/src/db/queries.rs에 모든 쿼리 집중
  - API, BuildExecutor, Deployer가 직접 queries 모듈 호출
  - Repository 패턴 미적용

문제점:
  1. 테스트 어려움: 실제 SQLite 필요
  2. 로직 중복: 여러 곳에서 유사한 DB 로직 반복
  3. 변경 어려움: DB 스키마 변경 시 여러 곳 수정 필요

필요한 해결책:
  - Repository trait 정의 (ProjectRepository, BuildRepository)
  - SqliteRepository 구현
  - MockRepository 작성 (테스트용)
  - 서비스 레이어에서만 Repository 사용

파일: agent/src/db/queries.rs

❌ 문제 20: API 명세 불일치
--------------------------------------------------------------------------------
원인: 코드 변경 시 문서 업데이트 누락
현재 상태:
  - API_통신_명세서.txt가 실제 구현과 불일치
  - 새 API 추가 시 문서 업데이트 누락
  - 에러 응답 포맷 불일치

문제점:
  1. 프론트엔드 개발 어려움: 실제 API와 문서가 다름
  2. 협업 어려움: 다른 개발자가 API 파악 어려움
  3. 유지보수 어려움: 문서 신뢰도 낮음

필요한 해결책:
  - OpenAPI 3.0 스펙 자동 생성 (utoipa 크레이트)
  - 코드에서 문서 생성 (Single Source of Truth)
  - API 변경 시 자동으로 문서 업데이트

파일: API_통신_명세서.txt (자동 생성으로 대체 필요)

✅ 리팩토링 계획 (Phase 11)
--------------------------------------------------------------------------------
위 문제들을 해결하기 위한 리팩토링 계획:

1. Trait 기반 의존성 주입 (문제 14, 17, 19 해결)
   - ProjectRepository, BuildRepository, SettingsRepository trait
   - ContainerRuntime trait
   - EventBus trait

2. Application Service 레이어 분리 (문제 15 해결)
   - ProjectService, BuildService, DeploymentService
   - 비즈니스 로직과 HTTP 핸들러 분리

3. 통합 로깅 시스템 (문제 16, 18 해결)
   - BoundaryLogger 구현
   - Trace ID 전파
   - 모든 모듈 경계에서 로깅

4. API 문서 자동 생성 (문제 20 해결)
   - OpenAPI 3.0 스펙
   - 코드에서 문서 생성

상세 계획: 개발_진행_상황.txt Phase 11 참조

교훈:
  - 초기 설계부터 테스트 가능성 고려 필요
  - 명확한 레이어 분리 (API, Application, Infrastructure)
  - 관찰 가능성(Observability) 설계 필수 (로깅, 추적)
  - 문서는 코드에서 자동 생성

================================================================================
Phase 5-6 전환 후 발견된 문제 (2026-01-10)
================================================================================

❌ 문제 21: ws_broadcaster.rs 브로드캐스트 채널 에러 미처리
--------------------------------------------------------------------------------
원인: broadcast::Receiver::recv()는 Result를 반환하지만 Err 케이스를 처리하지 않음
증상: 모든 이벤트 송신자가 드롭되면 WebSocket 브로드캐스팅이 조용히 중단됨
위치: agent/src/ws_broadcaster.rs:14
해결: match 패턴으로 Ok/Err 명시적 처리, Err 시 에러 로그 및 함수 종료

수정 코드:
```rust
event_result = event_rx.recv() => {
    match event_result {
        Ok(event) => {
            context.ws_connections.broadcast_event(&event).await;
        }
        Err(e) => {
            error!("Event broadcast channel error: {:?}", e);
            return Err(anyhow::anyhow!("Event channel closed: {:?}", e));
        }
    }
}
```

❌ 문제 22: 프로젝트 워크스페이스 경로 불일치
--------------------------------------------------------------------------------
원인: API와 서비스에서 다른 경로 사용 (/workspace/projects vs /data/workspace)
증상: Git 정보 수집 시 존재하지 않는 경로 탐색으로 빈 커밋 해시 반환
위치:
  - agent/src/api/projects.rs:163
  - agent/src/application/services/project_service.rs:112

해결: 모든 위치에서 `/data/workspace/{project_name}` 통일
  - `/workspace/projects/{name}` → `/data/workspace/{name}`

❌ 문제 23: build/worker.rs 순차 처리 Race Condition
--------------------------------------------------------------------------------
원인: finish_processing() 직후 즉시 다음 빌드가 시작될 수 있음
증상: 동일 프로젝트의 순차 처리 보장이 미세하게 깨질 수 있음
위치: agent/src/build/worker.rs:42

해결: finish_processing() 후 100ms 딜레이 추가
```rust
ctx.build_queue.finish_processing(project_id).await;
sleep(Duration::from_millis(100)).await;
```

❌ 문제 24: proxy/router.rs Response 생성 unwrap() 남용
--------------------------------------------------------------------------------
원인: Response::builder().body().unwrap() 호출이 10곳에서 발생
증상: Body 구성 실패 시 panic 발생 가능, 프록시 전체 크래시 위험
위치: agent/src/proxy/router.rs (라인 82, 92, 103, 113, 127, 135, 196, 224, 240, 267)

해결:
  1. error_response() 헬퍼 함수 추가
     - StatusCode와 message를 받아 안전하게 Response 생성
     - 실패 시 적절한 hyper::Error 반환

  2. 모든 unwrap()을 다음으로 교체:
     - error_response() 헬퍼 사용 (9곳)
     - map_err() 체이닝 (1곳, 최종 response 빌더)

수정 예시:
```rust
// Before:
return Ok(Response::builder()
    .status(StatusCode::NOT_FOUND)
    .header("Content-Type", "text/plain; charset=utf-8")
    .body(Full::new(Bytes::from("Not found")))
    .unwrap());

// After:
return error_response(StatusCode::NOT_FOUND, "Not found");
```

✅ 핵심 교훈 (Phase 5-6)
--------------------------------------------------------------------------------
1. **채널 에러 처리**: 모든 Result는 명시적 처리, 특히 채널/스트림 종료 케이스
2. **경로 일관성**: 하드코딩된 경로는 상수화하여 전체 일관성 보장
3. **동시성 제어**: 상태 전환 후 작은 딜레이로 race condition 방지
4. **Panic 방지**: unwrap() 대신 적절한 에러 처리 (특히 프록시 같은 핵심 경로)
5. **에러 전파**: HTTP 경계에서 적절한 에러 응답 생성

✅ 권장 개선 사항
--------------------------------------------------------------------------------
- 프로젝트 경로를 환경 변수 또는 config 파일로 중앙 관리
- BuildQueue에 명시적 동기화 메커니즘 추가 고려
- Response 빌더 패턴에 타입 안전성 래퍼 추가
- 모든 핵심 경로에서 unwrap() 사용 금지 (컴파일러 경고 추가 고려)
